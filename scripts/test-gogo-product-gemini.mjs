/**
 * DISABLED — Gemini product extract smoke test.
 * Gemini was removed from the production request path (security/cost hardening).
 * Do not re-enable without an explicit product decision and a non-public key.
 */
console.error('DISABLED: Gemini product test is intentionally unavailable.');
process.exit(1);
