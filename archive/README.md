# Archive

Files here are **not part of the running application**. They were moved from the project root to keep the repo tidy.

- **legacy-patches/** — historical one-off edit scripts used during development
- **media/** — large presentation/video files
- **diffs/** — scratch diffs and notes

Safe to delete if you no longer need local history.
